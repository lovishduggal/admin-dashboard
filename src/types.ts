export type Credentials = {
    email: string;
    password: string;
};

export type UserData = {
    id: string;
    email: string;
    createdAt: string;
    firstName: string;
    lastName: string;
};
