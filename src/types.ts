export type Tenant = {
    id: number;
    name: string;
    address: string;
};

export type Credentials = {
    email: string;
    password: string;
};

export type UserData = {
    id: string;
    email: string;
    createdAt: string;
    firstName: string;
    lastName: string;
};
